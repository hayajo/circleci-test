version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      - run:
          name: Print the Current Time
          command: date
  deploy:
    docker:
      - image: debian:stretch
    steps:
      - run:
          name: Print tag
          command: echo ${CIRCLE_TAG}


workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build:
      - deploy:
          filters:
            tags:
              only: /^[0-9.]+(?:-[0-9]+)?$/
            branches:
              ignore: /.*/

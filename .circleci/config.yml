version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      - run:
          name: Print the Current Time
          command: date
  pre_deploy:
    docker:
      - image: debian:stretch
    steps:
      - run:
          name: Print new version
          command: echo ${CIRCLE_BRANCH} | sed 's/.*-\([0-9.]\+\)$/\1/'
  deploy:
    docker:
      - image: debian:stretch
    steps:
      - run:
          name: Print tag
          command: echo ${CIRCLE_TAG}


workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build:
          filters:
            tags:
              only: /.*/
            branches:
              only: /.*/
      - pre_deploy:
          requires:
            - build
          filters:
            branches:
              only: /^bump-version-[0-9.]+$/
      - deploy:
          requires:
            - build
          filters:
            tags:
              only: /^v[0-9.]+(?:-[0-9]+)?$/
            branches:
              ignore: /.*/
